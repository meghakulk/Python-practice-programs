PROBLEM STATEMENT : Create a class with a class atribute a,create an object from it and set a directly using object.a =0.Does this change the class attribute
class rem:
    a =5
    
o = rem()
print(o.a) #prints the class attribute because instance attribute is not present
o.a = 0  #Instance attribute is set
print(o.a) # Prints the instance attribute because instance attribute is present
print(rem.a) #prints the class attribute

OUTPUT OF THE PROBLEM : 5
                        0
                        5
